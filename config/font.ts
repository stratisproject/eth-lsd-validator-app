import { Roboto } from "next/font/google";

export const roboto = Roboto({ weight: "400", subsets: ["latin"] });

export const robotoSemiBold = Roboto({ weight: "500", subsets: ["latin"] });

export const robotoBold = Roboto({ weight: "700", subsets: ["latin"] });
